
 function master(){
    var hw_make = document.getElementById("hw_make").value;   
    if(hw_make == ""){
          alert('make id must be filled');
    return false;
 }
 
 var hw_model = document.getElementById("hw_model").value;   
    if(hw_model == ""){
          alert('model id must be filled');
    return false;
 }
 
 var hw_processor = document.getElementById("hw_processor").value;   
    if(hw_processor == ""){
          alert('processor id must be filled');
    return false;
 }
 
 var hw_ram = document.getElementById("hw_ram").value;   
    if(hw_ram == ""){
          alert('ram id must be filled');
    return false;
 }
 
 
 var hw_Hdd = document.getElementById("hw_Hdd").value;   
    if(hw_ram == ""){
          alert('Hdd id must be filled');
    return false;
 }
 
 
 var hw_type = document.getElementById("hw_type").value;   
    if(hw_type == ""){
          alert('type id must be filled');
    return false;
 }
 
}